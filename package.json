{
  "name": "illuminazione-pubblica-monitor",
  "version": "4.0.0",
  "description": "Sistema multi-agente per il monitoraggio del procurement dell'illuminazione pubblica italiana",
  "type": "module",
  "scripts": {
    "run": "node scripts/pipeline.js",
    "dev": "node scripts/pipeline.js --dev"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "fast-xml-parser": "^4.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
